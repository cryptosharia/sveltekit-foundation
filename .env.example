# Public env vars (prefixed with `PUBLIC_`) are supposed to be exposed to the client.

# Local dev:
PUBLIC_APP_URL=http://localhost:5173
# Production example:
# PUBLIC_APP_URL=https://<app-subdomain>.cryptosharia.id

# CryptoSharia API base URL (public).
# Preview example:
PUBLIC_CS_API_URL=https://preview.api.cryptosharia.id
# Production example:
# PUBLIC_CS_API_URL=https://api.cryptosharia.id

# Private env vars (no `PUBLIC_` prefix) are server-only.

CS_API_KEY=
