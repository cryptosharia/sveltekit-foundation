// Minimal placeholder OpenAPI types for the foundation repo.
//
// Downstream app repos should overwrite this file by generating types from the CryptoSharia OpenAPI
// spec (see `rules/cryptosharia-api.md` and the `gen:api-types:*` scripts in `package.json`).

export type paths = Record<string, never>;
